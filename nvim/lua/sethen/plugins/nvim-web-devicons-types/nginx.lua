local palette = require('catppuccin.palettes').get_palette('mocha')

return {
    nginx = { icon = ' ', name = 'Nginx', color = palette.green },
    ['nginx.conf'] = { icon = ' ', name = 'NginxConf', color = palette.green },
}
