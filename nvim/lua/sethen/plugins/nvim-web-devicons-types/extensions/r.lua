local palette = require('catppuccin.palettes').get_palette('mocha')

local rIcon = ' '
local rColor = palette.red

return {
    r = { icon = rIcon, name = 'R', color = rColor },
    R = { icon = rIcon, name = 'R', color = rColor },
    rmd = { icon = rIcon, name = 'Rmd', color = rColor },
}
