local palette = require('catppuccin.palettes').get_palette('mocha')

local perlIcon = ' '
local perlColor = palette.blue

return {
    pl = { icon = perlIcon, name = 'Pl', color = perlColor },
    pm = { icon = perlIcon, name = 'Pm', color = perlColor },
}
