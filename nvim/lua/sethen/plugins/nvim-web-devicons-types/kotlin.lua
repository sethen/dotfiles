local palette = require('catppuccin.palettes').get_palette('mocha')

local ktIcon = ' '
local ktColor = palette.peach

return {
    kt = { icon = ktIcon, name = 'Kt', color = ktColor },
    kts = { icon = ktIcon, name = 'Kts', color = ktColor },
}
